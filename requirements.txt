# Core dependencies for DebateAgent
gradio>=4.0.0
httpx>=0.24.0
tenacity>=8.0.0
openai>=1.0.0
python-dotenv>=1.0.0

# Note: tokenizers dependency removed as it's not directly used in the codebase
# If needed for specific OpenAI functionality, uncomment the line below:
# tokenizers<=0.20.3,>=0.13.2

# Optional: OpenAI SDK (used as fallback in debate_app.py)
openai>=1.0.0